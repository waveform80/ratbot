<nav metal:define-macro="nav-bar">
  <div class="row">
    <div class="small-12 columns">
      <ul class="small-block-grid-2 medium-block-grid-4">
        <li tal:condition="request.matched_route.name not in (
          'index', 'issues', 'issue', 'page')">
          <a href="${request.route_url('index')}">
            <img height="36" src="${request.static_url('ratbot:static/home.opt.svg')}" />
            Home
          </a>
        </li>
        <li tal:condition="request.matched_route.name != 'comics'">
          <a href="${request.route_url('comics')}">
            <img height="36" src="${request.static_url('ratbot:static/comics.opt.svg')}" />
            Comics
          </a>
        </li>
        <li tal:condition="request.matched_route.name not in (
          'store', 'admin_index', 'admin_user_new', 'admin_user',
          'admin_comic_new', 'admin_comic', 'admin_issue_new', 'admin_issue',
          'admin_page_new', 'admin_page')">
          <a href="${view.site_store}">
            <img height="36" src="${request.static_url('ratbot:static/store.opt.svg')}" />
            Store
          </a>
        </li>
        <li tal:condition="request.matched_route.name != 'blog_issue'">
          <a href="${request.route_url('blog_index', comic='blog')}">
            <img height="36" src="${request.static_url('ratbot:static/blog.opt.svg')}" />
            Blog
          </a>
        </li>
        <li tal:condition="request.matched_route.name != 'bio'">
          <a href="${request.route_url('bio')}">
            <img height="36" src="${request.static_url('ratbot:static/bio.opt.svg')}" />
            Bio
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>
